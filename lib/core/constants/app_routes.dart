abstract final class AppRoutes {
  static const home = '/';
  static const search = '/search';
  static const movieDetail = '/movie-detail';

  /// Debug-only gallery for Step 2 widgets (see WidgetPreviewScreen).
  static const widgetPreview = '/dev/widget-preview';
}
